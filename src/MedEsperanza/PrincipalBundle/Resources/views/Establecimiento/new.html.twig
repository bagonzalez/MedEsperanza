<h1>Establecimiento creation</h1>

<form action="{{ path('establecimiento_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('establecimiento') }}">
            Back to the list
        </a>
    </li>
</ul>
